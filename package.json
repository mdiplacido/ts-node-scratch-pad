{
  "name": "ts-node-scratch-pad",
  "version": "1.0.0",
  "description": "ts-node scratch pad setup optimized for VS Code",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "repl": "ts-node"
  },
  "author": "Marco DiPlacido",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^8.0.53",
    "ts-node": "^3.3.0",
    "typescript": "^2.6.1"
  }
}
